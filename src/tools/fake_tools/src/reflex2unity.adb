with Ada.Text_IO; use Ada.Text_IO;
procedure Reflex2Unity is
   version : constant string := "0.2";
   target  : constant string := "unity";
begin
   Put_Line("reflex compiler v" & version & " - target " & target);
   Put_Line("**** all this is to ignore....");
end Reflex2Unity;
